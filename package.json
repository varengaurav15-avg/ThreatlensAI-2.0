{
  "name": "threatlens-ai",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"wait-on http://localhost:5173 && cd electron && npx electron .\"",
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:electron": "cd electron && npm install && npx electron-builder --win --config electron-builder.json",
    "build": "npm run build:frontend && npm run build:electron"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.5"
  }
}
