{
  "name": "threatlens-ai",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"wait-on http://localhost:5173 && cd electron && npx electron main.js\"",
    "build": "cd frontend && npm run build && cd ../electron && npx electron-builder"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "wait-on": "^9.0.5"
  }
}
